# Python framework examples

| Example | Stack | Command |
|---------|-------|---------|
| [fastmcp/](fastmcp/) | FastMCP | `python server.py` |
| [fastapi/](fastapi/) | FastAPI + MCP mount / SSE | `uvicorn server:app` (HTTP) or stdio wrapper |

Also use native `test_*.py` with `mcp_test_harness` assertions for multi-step flows.
